#!/usr/bin/env bash
set -euo pipefail

# MiniMax Codex compatibility stub.
# Usage: codex-mm [args]

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# shellcheck source=bin/lib/common.sh
source "${SCRIPT_DIR}/lib/common.sh"

exit_codex_paused
